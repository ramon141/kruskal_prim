package mintree;

import utils.set.ConjuntoDisjunto;

import java.util.ArrayList;

import graph.Edge;
import graph.Graph;
import graph.Vertex;

public class Kruskal {
	
	public static void exec(Graph graph) {
		ConjuntoDisjunto cd = new ConjuntoDisjunto();
		
		for(Vertex vertex: graph.vertices()) {
			cd.makeSet(vertex);
		}

		List<Edge> edgeSort = (new ArrayList<>(graph.edges()));
		
			
	}
	
	
	
	
	

}
